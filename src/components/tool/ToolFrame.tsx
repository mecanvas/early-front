import React from 'react';
import { FrameSize } from 'src/interfaces/ToolInterface';

interface Props {
  frameSize: FrameSize[];
}

const ToolFrame = ({ frameSize }: Props) => {
  return <div></div>;
};

export default ToolFrame;
